import Button from 'react-bootstrap/Button';
import './PrimaryButton.scss'

function PrimaryButton() {
  return (
      <Button variant="primary" className='primary-button'>Napisz do nas</Button>
  );
}

export default PrimaryButton;